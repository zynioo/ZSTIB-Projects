{
  "name": "project",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm start --prefix project-joker-backend",
    "build": "npm install --prefix project-joker-backend && npm install --prefix project-joker && npm run build --prefix project-joker"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
